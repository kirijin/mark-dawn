# mark-dawn pinned dependencies
# Shared by the container build, the macOS brew installer, and the Windows
# installer — every platform runs the same versions. Bump pins deliberately:
# `mark-dawn update` reinstalls whichever pins are current in the repo.

pymupdf4llm==1.28.0
markitdown[all]==0.1.6
watchdog==6.0.0
ocrmypdf==17.8.0
python-docx==1.2.0
